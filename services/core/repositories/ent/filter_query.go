// Code generated by ent, DO NOT EDIT.

package ent

import (
	"altron/core/repositories/ent/filter"
	"altron/core/repositories/ent/predicate"
	"altron/core/repositories/ent/service"
	"altron/core/repositories/ent/sessionfilter"
	"altron/core/repositories/ent/user"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// FilterQuery is the builder for querying Filter entities.
type FilterQuery struct {
	config
	ctx               *QueryContext
	order             []filter.OrderOption
	inters            []Interceptor
	predicates        []predicate.Filter
	withUser          *UserQuery
	withService       *ServiceQuery
	withSessionFilter *SessionFilterQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FilterQuery builder.
func (fq *FilterQuery) Where(ps ...predicate.Filter) *FilterQuery {
	fq.predicates = append(fq.predicates, ps...)
	return fq
}

// Limit the number of records to be returned by this query.
func (fq *FilterQuery) Limit(limit int) *FilterQuery {
	fq.ctx.Limit = &limit
	return fq
}

// Offset to start from.
func (fq *FilterQuery) Offset(offset int) *FilterQuery {
	fq.ctx.Offset = &offset
	return fq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fq *FilterQuery) Unique(unique bool) *FilterQuery {
	fq.ctx.Unique = &unique
	return fq
}

// Order specifies how the records should be ordered.
func (fq *FilterQuery) Order(o ...filter.OrderOption) *FilterQuery {
	fq.order = append(fq.order, o...)
	return fq
}

// QueryUser chains the current query on the "user" edge.
func (fq *FilterQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: fq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(filter.Table, filter.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, filter.UserTable, filter.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(fq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryService chains the current query on the "service" edge.
func (fq *FilterQuery) QueryService() *ServiceQuery {
	query := (&ServiceClient{config: fq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(filter.Table, filter.FieldID, selector),
			sqlgraph.To(service.Table, service.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, filter.ServiceTable, filter.ServiceColumn),
		)
		fromU = sqlgraph.SetNeighbors(fq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySessionFilter chains the current query on the "session_filter" edge.
func (fq *FilterQuery) QuerySessionFilter() *SessionFilterQuery {
	query := (&SessionFilterClient{config: fq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(filter.Table, filter.FieldID, selector),
			sqlgraph.To(sessionfilter.Table, sessionfilter.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, filter.SessionFilterTable, filter.SessionFilterColumn),
		)
		fromU = sqlgraph.SetNeighbors(fq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Filter entity from the query.
// Returns a *NotFoundError when no Filter was found.
func (fq *FilterQuery) First(ctx context.Context) (*Filter, error) {
	nodes, err := fq.Limit(1).All(setContextOp(ctx, fq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{filter.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fq *FilterQuery) FirstX(ctx context.Context) *Filter {
	node, err := fq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Filter ID from the query.
// Returns a *NotFoundError when no Filter ID was found.
func (fq *FilterQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = fq.Limit(1).IDs(setContextOp(ctx, fq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{filter.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fq *FilterQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := fq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Filter entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Filter entity is found.
// Returns a *NotFoundError when no Filter entities are found.
func (fq *FilterQuery) Only(ctx context.Context) (*Filter, error) {
	nodes, err := fq.Limit(2).All(setContextOp(ctx, fq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{filter.Label}
	default:
		return nil, &NotSingularError{filter.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fq *FilterQuery) OnlyX(ctx context.Context) *Filter {
	node, err := fq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Filter ID in the query.
// Returns a *NotSingularError when more than one Filter ID is found.
// Returns a *NotFoundError when no entities are found.
func (fq *FilterQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = fq.Limit(2).IDs(setContextOp(ctx, fq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{filter.Label}
	default:
		err = &NotSingularError{filter.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fq *FilterQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := fq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Filters.
func (fq *FilterQuery) All(ctx context.Context) ([]*Filter, error) {
	ctx = setContextOp(ctx, fq.ctx, "All")
	if err := fq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Filter, *FilterQuery]()
	return withInterceptors[[]*Filter](ctx, fq, qr, fq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fq *FilterQuery) AllX(ctx context.Context) []*Filter {
	nodes, err := fq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Filter IDs.
func (fq *FilterQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if fq.ctx.Unique == nil && fq.path != nil {
		fq.Unique(true)
	}
	ctx = setContextOp(ctx, fq.ctx, "IDs")
	if err = fq.Select(filter.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fq *FilterQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := fq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fq *FilterQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fq.ctx, "Count")
	if err := fq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fq, querierCount[*FilterQuery](), fq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fq *FilterQuery) CountX(ctx context.Context) int {
	count, err := fq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fq *FilterQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fq.ctx, "Exist")
	switch _, err := fq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fq *FilterQuery) ExistX(ctx context.Context) bool {
	exist, err := fq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FilterQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fq *FilterQuery) Clone() *FilterQuery {
	if fq == nil {
		return nil
	}
	return &FilterQuery{
		config:            fq.config,
		ctx:               fq.ctx.Clone(),
		order:             append([]filter.OrderOption{}, fq.order...),
		inters:            append([]Interceptor{}, fq.inters...),
		predicates:        append([]predicate.Filter{}, fq.predicates...),
		withUser:          fq.withUser.Clone(),
		withService:       fq.withService.Clone(),
		withSessionFilter: fq.withSessionFilter.Clone(),
		// clone intermediate query.
		sql:  fq.sql.Clone(),
		path: fq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (fq *FilterQuery) WithUser(opts ...func(*UserQuery)) *FilterQuery {
	query := (&UserClient{config: fq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fq.withUser = query
	return fq
}

// WithService tells the query-builder to eager-load the nodes that are connected to
// the "service" edge. The optional arguments are used to configure the query builder of the edge.
func (fq *FilterQuery) WithService(opts ...func(*ServiceQuery)) *FilterQuery {
	query := (&ServiceClient{config: fq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fq.withService = query
	return fq
}

// WithSessionFilter tells the query-builder to eager-load the nodes that are connected to
// the "session_filter" edge. The optional arguments are used to configure the query builder of the edge.
func (fq *FilterQuery) WithSessionFilter(opts ...func(*SessionFilterQuery)) *FilterQuery {
	query := (&SessionFilterClient{config: fq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fq.withSessionFilter = query
	return fq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID uuid.UUID `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Filter.Query().
//		GroupBy(filter.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fq *FilterQuery) GroupBy(field string, fields ...string) *FilterGroupBy {
	fq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FilterGroupBy{build: fq}
	grbuild.flds = &fq.ctx.Fields
	grbuild.label = filter.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID uuid.UUID `json:"user_id,omitempty"`
//	}
//
//	client.Filter.Query().
//		Select(filter.FieldUserID).
//		Scan(ctx, &v)
func (fq *FilterQuery) Select(fields ...string) *FilterSelect {
	fq.ctx.Fields = append(fq.ctx.Fields, fields...)
	sbuild := &FilterSelect{FilterQuery: fq}
	sbuild.label = filter.Label
	sbuild.flds, sbuild.scan = &fq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FilterSelect configured with the given aggregations.
func (fq *FilterQuery) Aggregate(fns ...AggregateFunc) *FilterSelect {
	return fq.Select().Aggregate(fns...)
}

func (fq *FilterQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fq); err != nil {
				return err
			}
		}
	}
	for _, f := range fq.ctx.Fields {
		if !filter.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fq.path != nil {
		prev, err := fq.path(ctx)
		if err != nil {
			return err
		}
		fq.sql = prev
	}
	return nil
}

func (fq *FilterQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Filter, error) {
	var (
		nodes       = []*Filter{}
		_spec       = fq.querySpec()
		loadedTypes = [3]bool{
			fq.withUser != nil,
			fq.withService != nil,
			fq.withSessionFilter != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Filter).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Filter{config: fq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := fq.withUser; query != nil {
		if err := fq.loadUser(ctx, query, nodes, nil,
			func(n *Filter, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := fq.withService; query != nil {
		if err := fq.loadService(ctx, query, nodes, nil,
			func(n *Filter, e *Service) { n.Edges.Service = e }); err != nil {
			return nil, err
		}
	}
	if query := fq.withSessionFilter; query != nil {
		if err := fq.loadSessionFilter(ctx, query, nodes,
			func(n *Filter) { n.Edges.SessionFilter = []*SessionFilter{} },
			func(n *Filter, e *SessionFilter) { n.Edges.SessionFilter = append(n.Edges.SessionFilter, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (fq *FilterQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*Filter, init func(*Filter), assign func(*Filter, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Filter)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (fq *FilterQuery) loadService(ctx context.Context, query *ServiceQuery, nodes []*Filter, init func(*Filter), assign func(*Filter, *Service)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Filter)
	for i := range nodes {
		fk := nodes[i].ServiceID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(service.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "service_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (fq *FilterQuery) loadSessionFilter(ctx context.Context, query *SessionFilterQuery, nodes []*Filter, init func(*Filter), assign func(*Filter, *SessionFilter)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Filter)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(sessionfilter.FieldFilterID)
	}
	query.Where(predicate.SessionFilter(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(filter.SessionFilterColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.FilterID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "filter_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (fq *FilterQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fq.querySpec()
	_spec.Node.Columns = fq.ctx.Fields
	if len(fq.ctx.Fields) > 0 {
		_spec.Unique = fq.ctx.Unique != nil && *fq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fq.driver, _spec)
}

func (fq *FilterQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(filter.Table, filter.Columns, sqlgraph.NewFieldSpec(filter.FieldID, field.TypeUUID))
	_spec.From = fq.sql
	if unique := fq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fq.path != nil {
		_spec.Unique = true
	}
	if fields := fq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, filter.FieldID)
		for i := range fields {
			if fields[i] != filter.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if fq.withUser != nil {
			_spec.Node.AddColumnOnce(filter.FieldUserID)
		}
		if fq.withService != nil {
			_spec.Node.AddColumnOnce(filter.FieldServiceID)
		}
	}
	if ps := fq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fq *FilterQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fq.driver.Dialect())
	t1 := builder.Table(filter.Table)
	columns := fq.ctx.Fields
	if len(columns) == 0 {
		columns = filter.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fq.sql != nil {
		selector = fq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fq.ctx.Unique != nil && *fq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fq.predicates {
		p(selector)
	}
	for _, p := range fq.order {
		p(selector)
	}
	if offset := fq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FilterGroupBy is the group-by builder for Filter entities.
type FilterGroupBy struct {
	selector
	build *FilterQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fgb *FilterGroupBy) Aggregate(fns ...AggregateFunc) *FilterGroupBy {
	fgb.fns = append(fgb.fns, fns...)
	return fgb
}

// Scan applies the selector query and scans the result into the given value.
func (fgb *FilterGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fgb.build.ctx, "GroupBy")
	if err := fgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FilterQuery, *FilterGroupBy](ctx, fgb.build, fgb, fgb.build.inters, v)
}

func (fgb *FilterGroupBy) sqlScan(ctx context.Context, root *FilterQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fgb.fns))
	for _, fn := range fgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fgb.flds)+len(fgb.fns))
		for _, f := range *fgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FilterSelect is the builder for selecting fields of Filter entities.
type FilterSelect struct {
	*FilterQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fs *FilterSelect) Aggregate(fns ...AggregateFunc) *FilterSelect {
	fs.fns = append(fs.fns, fns...)
	return fs
}

// Scan applies the selector query and scans the result into the given value.
func (fs *FilterSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fs.ctx, "Select")
	if err := fs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FilterQuery, *FilterSelect](ctx, fs.FilterQuery, fs, fs.inters, v)
}

func (fs *FilterSelect) sqlScan(ctx context.Context, root *FilterQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fs.fns))
	for _, fn := range fs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
