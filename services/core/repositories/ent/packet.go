// Code generated by ent, DO NOT EDIT.

package ent

import (
	"altron/core/repositories/ent/packet"
	"altron/core/repositories/ent/session"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// Packet is the model entity for the Packet schema.
type Packet struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// SessionID holds the value of the "session_id" field.
	SessionID uuid.UUID `json:"session_id,omitempty"`
	// SentAt holds the value of the "sent_at" field.
	SentAt time.Time `json:"sent_at,omitempty"`
	// IsRequest holds the value of the "is_request" field.
	IsRequest bool `json:"is_request,omitempty"`
	// Payload holds the value of the "payload" field.
	Payload string `json:"payload,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PacketQuery when eager-loading is set.
	Edges        PacketEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PacketEdges holds the relations/edges for other nodes in the graph.
type PacketEdges struct {
	// Session holds the value of the session edge.
	Session *Session `json:"session,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// SessionOrErr returns the Session value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PacketEdges) SessionOrErr() (*Session, error) {
	if e.loadedTypes[0] {
		if e.Session == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: session.Label}
		}
		return e.Session, nil
	}
	return nil, &NotLoadedError{edge: "session"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Packet) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case packet.FieldIsRequest:
			values[i] = new(sql.NullBool)
		case packet.FieldPayload:
			values[i] = new(sql.NullString)
		case packet.FieldSentAt:
			values[i] = new(sql.NullTime)
		case packet.FieldID, packet.FieldSessionID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Packet fields.
func (pa *Packet) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case packet.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				pa.ID = *value
			}
		case packet.FieldSessionID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field session_id", values[i])
			} else if value != nil {
				pa.SessionID = *value
			}
		case packet.FieldSentAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field sent_at", values[i])
			} else if value.Valid {
				pa.SentAt = value.Time
			}
		case packet.FieldIsRequest:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_request", values[i])
			} else if value.Valid {
				pa.IsRequest = value.Bool
			}
		case packet.FieldPayload:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field payload", values[i])
			} else if value.Valid {
				pa.Payload = value.String
			}
		default:
			pa.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Packet.
// This includes values selected through modifiers, order, etc.
func (pa *Packet) Value(name string) (ent.Value, error) {
	return pa.selectValues.Get(name)
}

// QuerySession queries the "session" edge of the Packet entity.
func (pa *Packet) QuerySession() *SessionQuery {
	return NewPacketClient(pa.config).QuerySession(pa)
}

// Update returns a builder for updating this Packet.
// Note that you need to call Packet.Unwrap() before calling this method if this Packet
// was returned from a transaction, and the transaction was committed or rolled back.
func (pa *Packet) Update() *PacketUpdateOne {
	return NewPacketClient(pa.config).UpdateOne(pa)
}

// Unwrap unwraps the Packet entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pa *Packet) Unwrap() *Packet {
	_tx, ok := pa.config.driver.(*txDriver)
	if !ok {
		panic("ent: Packet is not a transactional entity")
	}
	pa.config.driver = _tx.drv
	return pa
}

// String implements the fmt.Stringer.
func (pa *Packet) String() string {
	var builder strings.Builder
	builder.WriteString("Packet(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pa.ID))
	builder.WriteString("session_id=")
	builder.WriteString(fmt.Sprintf("%v", pa.SessionID))
	builder.WriteString(", ")
	builder.WriteString("sent_at=")
	builder.WriteString(pa.SentAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("is_request=")
	builder.WriteString(fmt.Sprintf("%v", pa.IsRequest))
	builder.WriteString(", ")
	builder.WriteString("payload=")
	builder.WriteString(pa.Payload)
	builder.WriteByte(')')
	return builder.String()
}

// Packets is a parsable slice of Packet.
type Packets []*Packet
