// Code generated by ent, DO NOT EDIT.

package ent

import (
	"altron/core/repositories/ent/analyzercomponent"
	"altron/core/repositories/ent/analyzerpayload"
	"altron/core/repositories/ent/predicate"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// AnalyzerComponentQuery is the builder for querying AnalyzerComponent entities.
type AnalyzerComponentQuery struct {
	config
	ctx                  *QueryContext
	order                []analyzercomponent.OrderOption
	inters               []Interceptor
	predicates           []predicate.AnalyzerComponent
	withAnalyzerPayloads *AnalyzerPayloadQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AnalyzerComponentQuery builder.
func (acq *AnalyzerComponentQuery) Where(ps ...predicate.AnalyzerComponent) *AnalyzerComponentQuery {
	acq.predicates = append(acq.predicates, ps...)
	return acq
}

// Limit the number of records to be returned by this query.
func (acq *AnalyzerComponentQuery) Limit(limit int) *AnalyzerComponentQuery {
	acq.ctx.Limit = &limit
	return acq
}

// Offset to start from.
func (acq *AnalyzerComponentQuery) Offset(offset int) *AnalyzerComponentQuery {
	acq.ctx.Offset = &offset
	return acq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (acq *AnalyzerComponentQuery) Unique(unique bool) *AnalyzerComponentQuery {
	acq.ctx.Unique = &unique
	return acq
}

// Order specifies how the records should be ordered.
func (acq *AnalyzerComponentQuery) Order(o ...analyzercomponent.OrderOption) *AnalyzerComponentQuery {
	acq.order = append(acq.order, o...)
	return acq
}

// QueryAnalyzerPayloads chains the current query on the "analyzer_payloads" edge.
func (acq *AnalyzerComponentQuery) QueryAnalyzerPayloads() *AnalyzerPayloadQuery {
	query := (&AnalyzerPayloadClient{config: acq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := acq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := acq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(analyzercomponent.Table, analyzercomponent.FieldID, selector),
			sqlgraph.To(analyzerpayload.Table, analyzerpayload.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, analyzercomponent.AnalyzerPayloadsTable, analyzercomponent.AnalyzerPayloadsColumn),
		)
		fromU = sqlgraph.SetNeighbors(acq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AnalyzerComponent entity from the query.
// Returns a *NotFoundError when no AnalyzerComponent was found.
func (acq *AnalyzerComponentQuery) First(ctx context.Context) (*AnalyzerComponent, error) {
	nodes, err := acq.Limit(1).All(setContextOp(ctx, acq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{analyzercomponent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (acq *AnalyzerComponentQuery) FirstX(ctx context.Context) *AnalyzerComponent {
	node, err := acq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AnalyzerComponent ID from the query.
// Returns a *NotFoundError when no AnalyzerComponent ID was found.
func (acq *AnalyzerComponentQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = acq.Limit(1).IDs(setContextOp(ctx, acq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{analyzercomponent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (acq *AnalyzerComponentQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := acq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AnalyzerComponent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AnalyzerComponent entity is found.
// Returns a *NotFoundError when no AnalyzerComponent entities are found.
func (acq *AnalyzerComponentQuery) Only(ctx context.Context) (*AnalyzerComponent, error) {
	nodes, err := acq.Limit(2).All(setContextOp(ctx, acq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{analyzercomponent.Label}
	default:
		return nil, &NotSingularError{analyzercomponent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (acq *AnalyzerComponentQuery) OnlyX(ctx context.Context) *AnalyzerComponent {
	node, err := acq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AnalyzerComponent ID in the query.
// Returns a *NotSingularError when more than one AnalyzerComponent ID is found.
// Returns a *NotFoundError when no entities are found.
func (acq *AnalyzerComponentQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = acq.Limit(2).IDs(setContextOp(ctx, acq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{analyzercomponent.Label}
	default:
		err = &NotSingularError{analyzercomponent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (acq *AnalyzerComponentQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := acq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AnalyzerComponents.
func (acq *AnalyzerComponentQuery) All(ctx context.Context) ([]*AnalyzerComponent, error) {
	ctx = setContextOp(ctx, acq.ctx, "All")
	if err := acq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AnalyzerComponent, *AnalyzerComponentQuery]()
	return withInterceptors[[]*AnalyzerComponent](ctx, acq, qr, acq.inters)
}

// AllX is like All, but panics if an error occurs.
func (acq *AnalyzerComponentQuery) AllX(ctx context.Context) []*AnalyzerComponent {
	nodes, err := acq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AnalyzerComponent IDs.
func (acq *AnalyzerComponentQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if acq.ctx.Unique == nil && acq.path != nil {
		acq.Unique(true)
	}
	ctx = setContextOp(ctx, acq.ctx, "IDs")
	if err = acq.Select(analyzercomponent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (acq *AnalyzerComponentQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := acq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (acq *AnalyzerComponentQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, acq.ctx, "Count")
	if err := acq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, acq, querierCount[*AnalyzerComponentQuery](), acq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (acq *AnalyzerComponentQuery) CountX(ctx context.Context) int {
	count, err := acq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (acq *AnalyzerComponentQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, acq.ctx, "Exist")
	switch _, err := acq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (acq *AnalyzerComponentQuery) ExistX(ctx context.Context) bool {
	exist, err := acq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AnalyzerComponentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (acq *AnalyzerComponentQuery) Clone() *AnalyzerComponentQuery {
	if acq == nil {
		return nil
	}
	return &AnalyzerComponentQuery{
		config:               acq.config,
		ctx:                  acq.ctx.Clone(),
		order:                append([]analyzercomponent.OrderOption{}, acq.order...),
		inters:               append([]Interceptor{}, acq.inters...),
		predicates:           append([]predicate.AnalyzerComponent{}, acq.predicates...),
		withAnalyzerPayloads: acq.withAnalyzerPayloads.Clone(),
		// clone intermediate query.
		sql:  acq.sql.Clone(),
		path: acq.path,
	}
}

// WithAnalyzerPayloads tells the query-builder to eager-load the nodes that are connected to
// the "analyzer_payloads" edge. The optional arguments are used to configure the query builder of the edge.
func (acq *AnalyzerComponentQuery) WithAnalyzerPayloads(opts ...func(*AnalyzerPayloadQuery)) *AnalyzerComponentQuery {
	query := (&AnalyzerPayloadClient{config: acq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	acq.withAnalyzerPayloads = query
	return acq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AnalyzerComponent.Query().
//		GroupBy(analyzercomponent.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (acq *AnalyzerComponentQuery) GroupBy(field string, fields ...string) *AnalyzerComponentGroupBy {
	acq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AnalyzerComponentGroupBy{build: acq}
	grbuild.flds = &acq.ctx.Fields
	grbuild.label = analyzercomponent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.AnalyzerComponent.Query().
//		Select(analyzercomponent.FieldName).
//		Scan(ctx, &v)
func (acq *AnalyzerComponentQuery) Select(fields ...string) *AnalyzerComponentSelect {
	acq.ctx.Fields = append(acq.ctx.Fields, fields...)
	sbuild := &AnalyzerComponentSelect{AnalyzerComponentQuery: acq}
	sbuild.label = analyzercomponent.Label
	sbuild.flds, sbuild.scan = &acq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AnalyzerComponentSelect configured with the given aggregations.
func (acq *AnalyzerComponentQuery) Aggregate(fns ...AggregateFunc) *AnalyzerComponentSelect {
	return acq.Select().Aggregate(fns...)
}

func (acq *AnalyzerComponentQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range acq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, acq); err != nil {
				return err
			}
		}
	}
	for _, f := range acq.ctx.Fields {
		if !analyzercomponent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if acq.path != nil {
		prev, err := acq.path(ctx)
		if err != nil {
			return err
		}
		acq.sql = prev
	}
	return nil
}

func (acq *AnalyzerComponentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AnalyzerComponent, error) {
	var (
		nodes       = []*AnalyzerComponent{}
		_spec       = acq.querySpec()
		loadedTypes = [1]bool{
			acq.withAnalyzerPayloads != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AnalyzerComponent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AnalyzerComponent{config: acq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, acq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := acq.withAnalyzerPayloads; query != nil {
		if err := acq.loadAnalyzerPayloads(ctx, query, nodes,
			func(n *AnalyzerComponent) { n.Edges.AnalyzerPayloads = []*AnalyzerPayload{} },
			func(n *AnalyzerComponent, e *AnalyzerPayload) {
				n.Edges.AnalyzerPayloads = append(n.Edges.AnalyzerPayloads, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (acq *AnalyzerComponentQuery) loadAnalyzerPayloads(ctx context.Context, query *AnalyzerPayloadQuery, nodes []*AnalyzerComponent, init func(*AnalyzerComponent), assign func(*AnalyzerComponent, *AnalyzerPayload)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*AnalyzerComponent)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(analyzerpayload.FieldAnalyzerComponentID)
	}
	query.Where(predicate.AnalyzerPayload(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(analyzercomponent.AnalyzerPayloadsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AnalyzerComponentID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "analyzer_component_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (acq *AnalyzerComponentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := acq.querySpec()
	_spec.Node.Columns = acq.ctx.Fields
	if len(acq.ctx.Fields) > 0 {
		_spec.Unique = acq.ctx.Unique != nil && *acq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, acq.driver, _spec)
}

func (acq *AnalyzerComponentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(analyzercomponent.Table, analyzercomponent.Columns, sqlgraph.NewFieldSpec(analyzercomponent.FieldID, field.TypeUUID))
	_spec.From = acq.sql
	if unique := acq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if acq.path != nil {
		_spec.Unique = true
	}
	if fields := acq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, analyzercomponent.FieldID)
		for i := range fields {
			if fields[i] != analyzercomponent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := acq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := acq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := acq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := acq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (acq *AnalyzerComponentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(acq.driver.Dialect())
	t1 := builder.Table(analyzercomponent.Table)
	columns := acq.ctx.Fields
	if len(columns) == 0 {
		columns = analyzercomponent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if acq.sql != nil {
		selector = acq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if acq.ctx.Unique != nil && *acq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range acq.predicates {
		p(selector)
	}
	for _, p := range acq.order {
		p(selector)
	}
	if offset := acq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := acq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AnalyzerComponentGroupBy is the group-by builder for AnalyzerComponent entities.
type AnalyzerComponentGroupBy struct {
	selector
	build *AnalyzerComponentQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (acgb *AnalyzerComponentGroupBy) Aggregate(fns ...AggregateFunc) *AnalyzerComponentGroupBy {
	acgb.fns = append(acgb.fns, fns...)
	return acgb
}

// Scan applies the selector query and scans the result into the given value.
func (acgb *AnalyzerComponentGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, acgb.build.ctx, "GroupBy")
	if err := acgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AnalyzerComponentQuery, *AnalyzerComponentGroupBy](ctx, acgb.build, acgb, acgb.build.inters, v)
}

func (acgb *AnalyzerComponentGroupBy) sqlScan(ctx context.Context, root *AnalyzerComponentQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(acgb.fns))
	for _, fn := range acgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*acgb.flds)+len(acgb.fns))
		for _, f := range *acgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*acgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := acgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AnalyzerComponentSelect is the builder for selecting fields of AnalyzerComponent entities.
type AnalyzerComponentSelect struct {
	*AnalyzerComponentQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (acs *AnalyzerComponentSelect) Aggregate(fns ...AggregateFunc) *AnalyzerComponentSelect {
	acs.fns = append(acs.fns, fns...)
	return acs
}

// Scan applies the selector query and scans the result into the given value.
func (acs *AnalyzerComponentSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, acs.ctx, "Select")
	if err := acs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AnalyzerComponentQuery, *AnalyzerComponentSelect](ctx, acs.AnalyzerComponentQuery, acs, acs.inters, v)
}

func (acs *AnalyzerComponentSelect) sqlScan(ctx context.Context, root *AnalyzerComponentQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(acs.fns))
	for _, fn := range acs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*acs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := acs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
