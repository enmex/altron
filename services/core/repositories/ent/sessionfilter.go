// Code generated by ent, DO NOT EDIT.

package ent

import (
	"altron/core/repositories/ent/filter"
	"altron/core/repositories/ent/session"
	"altron/core/repositories/ent/sessionfilter"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// SessionFilter is the model entity for the SessionFilter schema.
type SessionFilter struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// SessionID holds the value of the "session_id" field.
	SessionID uuid.UUID `json:"session_id,omitempty"`
	// FilterID holds the value of the "filter_id" field.
	FilterID uuid.UUID `json:"filter_id,omitempty"`
	// MatchesCount holds the value of the "matches_count" field.
	MatchesCount int `json:"matches_count,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SessionFilterQuery when eager-loading is set.
	Edges        SessionFilterEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SessionFilterEdges holds the relations/edges for other nodes in the graph.
type SessionFilterEdges struct {
	// Session holds the value of the session edge.
	Session *Session `json:"session,omitempty"`
	// Filter holds the value of the filter edge.
	Filter *Filter `json:"filter,omitempty"`
	// MatchedPackets holds the value of the matched_packets edge.
	MatchedPackets []*MatchedPacket `json:"matched_packets,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// SessionOrErr returns the Session value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SessionFilterEdges) SessionOrErr() (*Session, error) {
	if e.loadedTypes[0] {
		if e.Session == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: session.Label}
		}
		return e.Session, nil
	}
	return nil, &NotLoadedError{edge: "session"}
}

// FilterOrErr returns the Filter value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SessionFilterEdges) FilterOrErr() (*Filter, error) {
	if e.loadedTypes[1] {
		if e.Filter == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: filter.Label}
		}
		return e.Filter, nil
	}
	return nil, &NotLoadedError{edge: "filter"}
}

// MatchedPacketsOrErr returns the MatchedPackets value or an error if the edge
// was not loaded in eager-loading.
func (e SessionFilterEdges) MatchedPacketsOrErr() ([]*MatchedPacket, error) {
	if e.loadedTypes[2] {
		return e.MatchedPackets, nil
	}
	return nil, &NotLoadedError{edge: "matched_packets"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SessionFilter) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sessionfilter.FieldMatchesCount:
			values[i] = new(sql.NullInt64)
		case sessionfilter.FieldID, sessionfilter.FieldSessionID, sessionfilter.FieldFilterID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SessionFilter fields.
func (sf *SessionFilter) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sessionfilter.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				sf.ID = *value
			}
		case sessionfilter.FieldSessionID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field session_id", values[i])
			} else if value != nil {
				sf.SessionID = *value
			}
		case sessionfilter.FieldFilterID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field filter_id", values[i])
			} else if value != nil {
				sf.FilterID = *value
			}
		case sessionfilter.FieldMatchesCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field matches_count", values[i])
			} else if value.Valid {
				sf.MatchesCount = int(value.Int64)
			}
		default:
			sf.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SessionFilter.
// This includes values selected through modifiers, order, etc.
func (sf *SessionFilter) Value(name string) (ent.Value, error) {
	return sf.selectValues.Get(name)
}

// QuerySession queries the "session" edge of the SessionFilter entity.
func (sf *SessionFilter) QuerySession() *SessionQuery {
	return NewSessionFilterClient(sf.config).QuerySession(sf)
}

// QueryFilter queries the "filter" edge of the SessionFilter entity.
func (sf *SessionFilter) QueryFilter() *FilterQuery {
	return NewSessionFilterClient(sf.config).QueryFilter(sf)
}

// QueryMatchedPackets queries the "matched_packets" edge of the SessionFilter entity.
func (sf *SessionFilter) QueryMatchedPackets() *MatchedPacketQuery {
	return NewSessionFilterClient(sf.config).QueryMatchedPackets(sf)
}

// Update returns a builder for updating this SessionFilter.
// Note that you need to call SessionFilter.Unwrap() before calling this method if this SessionFilter
// was returned from a transaction, and the transaction was committed or rolled back.
func (sf *SessionFilter) Update() *SessionFilterUpdateOne {
	return NewSessionFilterClient(sf.config).UpdateOne(sf)
}

// Unwrap unwraps the SessionFilter entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sf *SessionFilter) Unwrap() *SessionFilter {
	_tx, ok := sf.config.driver.(*txDriver)
	if !ok {
		panic("ent: SessionFilter is not a transactional entity")
	}
	sf.config.driver = _tx.drv
	return sf
}

// String implements the fmt.Stringer.
func (sf *SessionFilter) String() string {
	var builder strings.Builder
	builder.WriteString("SessionFilter(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sf.ID))
	builder.WriteString("session_id=")
	builder.WriteString(fmt.Sprintf("%v", sf.SessionID))
	builder.WriteString(", ")
	builder.WriteString("filter_id=")
	builder.WriteString(fmt.Sprintf("%v", sf.FilterID))
	builder.WriteString(", ")
	builder.WriteString("matches_count=")
	builder.WriteString(fmt.Sprintf("%v", sf.MatchesCount))
	builder.WriteByte(')')
	return builder.String()
}

// SessionFilters is a parsable slice of SessionFilter.
type SessionFilters []*SessionFilter
