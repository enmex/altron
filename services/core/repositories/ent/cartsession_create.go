// Code generated by ent, DO NOT EDIT.

package ent

import (
	"altron/core/repositories/ent/cartsession"
	"altron/core/repositories/ent/session"
	"altron/core/repositories/ent/workspace"
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// CartSessionCreate is the builder for creating a CartSession entity.
type CartSessionCreate struct {
	config
	mutation *CartSessionMutation
	hooks    []Hook
}

// SetWorkspaceID sets the "workspace_id" field.
func (csc *CartSessionCreate) SetWorkspaceID(u uuid.UUID) *CartSessionCreate {
	csc.mutation.SetWorkspaceID(u)
	return csc
}

// SetSessionID sets the "session_id" field.
func (csc *CartSessionCreate) SetSessionID(u uuid.UUID) *CartSessionCreate {
	csc.mutation.SetSessionID(u)
	return csc
}

// SetID sets the "id" field.
func (csc *CartSessionCreate) SetID(u uuid.UUID) *CartSessionCreate {
	csc.mutation.SetID(u)
	return csc
}

// SetNillableID sets the "id" field if the given value is not nil.
func (csc *CartSessionCreate) SetNillableID(u *uuid.UUID) *CartSessionCreate {
	if u != nil {
		csc.SetID(*u)
	}
	return csc
}

// SetSession sets the "session" edge to the Session entity.
func (csc *CartSessionCreate) SetSession(s *Session) *CartSessionCreate {
	return csc.SetSessionID(s.ID)
}

// SetWorkspace sets the "workspace" edge to the Workspace entity.
func (csc *CartSessionCreate) SetWorkspace(w *Workspace) *CartSessionCreate {
	return csc.SetWorkspaceID(w.ID)
}

// Mutation returns the CartSessionMutation object of the builder.
func (csc *CartSessionCreate) Mutation() *CartSessionMutation {
	return csc.mutation
}

// Save creates the CartSession in the database.
func (csc *CartSessionCreate) Save(ctx context.Context) (*CartSession, error) {
	csc.defaults()
	return withHooks(ctx, csc.sqlSave, csc.mutation, csc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (csc *CartSessionCreate) SaveX(ctx context.Context) *CartSession {
	v, err := csc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (csc *CartSessionCreate) Exec(ctx context.Context) error {
	_, err := csc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (csc *CartSessionCreate) ExecX(ctx context.Context) {
	if err := csc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (csc *CartSessionCreate) defaults() {
	if _, ok := csc.mutation.ID(); !ok {
		v := cartsession.DefaultID()
		csc.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (csc *CartSessionCreate) check() error {
	if _, ok := csc.mutation.WorkspaceID(); !ok {
		return &ValidationError{Name: "workspace_id", err: errors.New(`ent: missing required field "CartSession.workspace_id"`)}
	}
	if _, ok := csc.mutation.SessionID(); !ok {
		return &ValidationError{Name: "session_id", err: errors.New(`ent: missing required field "CartSession.session_id"`)}
	}
	if _, ok := csc.mutation.SessionID(); !ok {
		return &ValidationError{Name: "session", err: errors.New(`ent: missing required edge "CartSession.session"`)}
	}
	if _, ok := csc.mutation.WorkspaceID(); !ok {
		return &ValidationError{Name: "workspace", err: errors.New(`ent: missing required edge "CartSession.workspace"`)}
	}
	return nil
}

func (csc *CartSessionCreate) sqlSave(ctx context.Context) (*CartSession, error) {
	if err := csc.check(); err != nil {
		return nil, err
	}
	_node, _spec := csc.createSpec()
	if err := sqlgraph.CreateNode(ctx, csc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*uuid.UUID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	csc.mutation.id = &_node.ID
	csc.mutation.done = true
	return _node, nil
}

func (csc *CartSessionCreate) createSpec() (*CartSession, *sqlgraph.CreateSpec) {
	var (
		_node = &CartSession{config: csc.config}
		_spec = sqlgraph.NewCreateSpec(cartsession.Table, sqlgraph.NewFieldSpec(cartsession.FieldID, field.TypeUUID))
	)
	if id, ok := csc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	if nodes := csc.mutation.SessionIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2O,
			Inverse: true,
			Table:   cartsession.SessionTable,
			Columns: []string{cartsession.SessionColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(session.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.SessionID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := csc.mutation.WorkspaceIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   cartsession.WorkspaceTable,
			Columns: []string{cartsession.WorkspaceColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(workspace.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.WorkspaceID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// CartSessionCreateBulk is the builder for creating many CartSession entities in bulk.
type CartSessionCreateBulk struct {
	config
	err      error
	builders []*CartSessionCreate
}

// Save creates the CartSession entities in the database.
func (cscb *CartSessionCreateBulk) Save(ctx context.Context) ([]*CartSession, error) {
	if cscb.err != nil {
		return nil, cscb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(cscb.builders))
	nodes := make([]*CartSession, len(cscb.builders))
	mutators := make([]Mutator, len(cscb.builders))
	for i := range cscb.builders {
		func(i int, root context.Context) {
			builder := cscb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*CartSessionMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, cscb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, cscb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, cscb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (cscb *CartSessionCreateBulk) SaveX(ctx context.Context) []*CartSession {
	v, err := cscb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (cscb *CartSessionCreateBulk) Exec(ctx context.Context) error {
	_, err := cscb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (cscb *CartSessionCreateBulk) ExecX(ctx context.Context) {
	if err := cscb.Exec(ctx); err != nil {
		panic(err)
	}
}
